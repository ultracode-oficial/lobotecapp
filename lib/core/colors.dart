import 'package:flutter/material.dart';

class AppColors {
  // Primary colors (Azul LoboRJ Oficial)
  static const Color primary = Color(0xFF003399); 
  static const Color primaryLight = Color(0xFF018DD1);
  static const Color primaryDark = Color(0xFF002266);

  // Accent / Secondary
  static const Color accent = Color(0xFF20C997); // Verde menta / frescor
  
  // Backgrounds (Off-white / Cinza muito claro)
  static const Color background = Color(0xFFF9FAFB); 
  static const Color cardBackground = Colors.white;

  // Typography (Chumbo do site)
  static const Color textPrimary = Color(0xFF3A3A3A); 
  static const Color textSecondary = Color(0xFF707070);

  // Status colors
  static const Color success = Color(0xFF28A745);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFDC3545);
  static const Color info = Color(0xFF17A2B8);

  // Borders & Dividers
  static const Color divider = Color(0xFFE9ECEF);
  static const Color border = Color(0xFFDEE2E6);
}
